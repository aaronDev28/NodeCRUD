# NodeJS and MySQL Database Connectivity

## Check if server is running, if the pages are not working properly.
## npm install to install the necessary modules
## node index.js after cd server, to run the server